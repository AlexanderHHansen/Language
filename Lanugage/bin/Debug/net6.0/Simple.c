#include <stdio.h>

#include <string.h>

#include <stdlib.h>

#include <math.h>

char* bool_to_str(int bool) { return bool == 1 ? "true" : "false"; }

int main() {

	int hello = (1 == 1);
	int i = 1;
	while (i < 10 && hello == 0false) {

		char* i = "hello";
		printf(%s, i))

;
}

;
}